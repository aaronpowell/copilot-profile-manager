<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="CopilotProfileManager.WinUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="180" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="16,16,16,0" Padding="20" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Spacing="8">
                <TextBlock FontSize="28" FontWeight="SemiBold" Text="Copilot Profile Manager" />
                <TextBlock Text="Manage GitHub Copilot Windows Terminal profiles, choose where they sync, and control the Explorer submenu without editing JSON or registry keys by hand." TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="16,16,16,0" Padding="20" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid ColumnSpacing="12" RowSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Profile" />
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding Profiles}"
                          SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                          DisplayMemberPath="Name" />

                <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <Button Command="{Binding CreateProfileCommand}" Content="New profile" />
                    <Button Command="{Binding DuplicateProfileCommand}" Content="Duplicate" />
                    <Button Command="{Binding DeleteProfileCommand}" Content="Delete" />
                    <Button Command="{Binding ReloadCommand}" Content="Reload from disk" />
                    <Button Command="{Binding SaveCommand}" Content="Save all changes" />
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                           Text="Profiles are merged by GUID across Windows Terminal and Windows Terminal Preview. Use the selector above to switch profiles, then edit the fields below."
                           TextWrapping="Wrap" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="16,16,16,0">
            <StackPanel Spacing="16">
                <Border Padding="20" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="16">
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Profile editor" />

                        <Grid ColumnSpacing="12" RowSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Profile name" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, Mode=TwoWay}" />

                            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Profile GUID" />
                            <TextBox Grid.Row="1" Grid.Column="1" IsReadOnly="True" Text="{Binding GuidText}" />

                            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Shell command prefix" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ShellCommandPrefix, Mode=TwoWay}" />

                            <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Text="Copilot CLI flags" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CopilotArguments, Mode=TwoWay}" />

                            <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="Command preview" />
                            <TextBox Grid.Row="4" Grid.Column="1" IsReadOnly="True" Text="{Binding CommandPreview}" TextWrapping="Wrap" />

                            <TextBlock Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Text="Color scheme" />
                            <ComboBox Grid.Row="5" Grid.Column="1"
                                      ItemsSource="{Binding ColorSchemes}"
                                      SelectedItem="{Binding ColorScheme, Mode=TwoWay}" />

                            <TextBlock Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Text="Starting directory" />
                            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding StartingDirectory, Mode=TwoWay}" />

                            <TextBlock Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Text="Icon path" />
                            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding IconPath, Mode=TwoWay}" />

                            <TextBlock Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Text="Background image" />
                            <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding BackgroundImagePath, Mode=TwoWay}" />

                            <TextBlock Grid.Row="9" Grid.Column="0" VerticalAlignment="Center" Text="Background opacity" />
                            <NumberBox Grid.Row="9" Grid.Column="1" Minimum="0" Maximum="1" SmallChange="0.05" Value="{Binding BackgroundOpacity, Mode=TwoWay}" />

                            <TextBlock Grid.Row="10" Grid.Column="0" VerticalAlignment="Center" Text="Window opacity" />
                            <NumberBox Grid.Row="10" Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding WindowOpacity, Mode=TwoWay}" />

                            <TextBlock Grid.Row="11" Grid.Column="0" VerticalAlignment="Center" Text="Tab title" />
                            <TextBox Grid.Row="11" Grid.Column="1" Text="{Binding TabTitle, Mode=TwoWay}" />
                        </Grid>

                        <CheckBox Content="Hide this profile from the Terminal profile list" IsChecked="{Binding Hidden, Mode=TwoWay}" />
                    </StackPanel>
                </Border>

                <Border Padding="20" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Sync targets" />
                        <TextBlock Text="Choose which Terminal settings files receive this profile, and whether the Explorer Copilot submenu should be published." TextWrapping="Wrap" />

                        <CheckBox Content="Windows Terminal (stable)" IsEnabled="{Binding IsStableAvailable}" IsChecked="{Binding SyncStable, Mode=TwoWay}" />
                        <TextBlock Margin="28,0,0,0" Opacity="0.8" Text="{Binding StableSettingsPath}" TextWrapping="Wrap" />

                        <CheckBox Content="Windows Terminal Preview" IsEnabled="{Binding IsPreviewAvailable}" IsChecked="{Binding SyncPreview, Mode=TwoWay}" />
                        <TextBlock Margin="28,0,0,0" Opacity="0.8" Text="{Binding PreviewSettingsPath}" TextWrapping="Wrap" />

                        <CheckBox Content="Explorer submenu" IsChecked="{Binding SyncRegistry, Mode=TwoWay}" />
                        <TextBlock Margin="28,0,0,0" Opacity="0.8" Text="Adds or removes the classic Explorer submenu for folders and folder backgrounds." TextWrapping="Wrap" />

                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button Command="{Binding RemoveExplorerMenuCommand}" Content="Remove Explorer menu now" />
                            <ProgressRing Width="20" Height="20" IsActive="{Binding IsBusy}" />
                        </StackPanel>

                        <TextBlock Text="To remove registry changes during uninstall, click 'Remove Explorer menu now'. That clears the current submenu immediately and turns off Explorer sync for every profile in the editor." TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Padding="20" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Flag helper" />
                        <TextBlock Text="Use this helper to discover Copilot CLI flags, then insert them into the selected profile's flags field." TextWrapping="Wrap" />
                        <ListView Height="220"
                                  ItemsSource="{Binding CliOptions}"
                                  SelectedItem="{Binding SelectedCliOption, Mode=TwoWay}"
                                  DisplayMemberPath="Syntax" />
                        <TextBox Height="140"
                                 AcceptsReturn="True"
                                 IsReadOnly="True"
                                 Text="{Binding SelectedCliOptionDescription}"
                                 TextWrapping="Wrap" />
                        <Button HorizontalAlignment="Right" Command="{Binding InsertSelectedFlagCommand}" Content="Insert into selected profile" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="3" Margin="16,16,16,16" Padding="16" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Spacing="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="Activity" />
                    <Button Grid.Column="1" Command="{Binding ClearActivityLogCommand}" Content="Clear log" />
                </Grid>
                <TextBox AcceptsReturn="True"
                         IsReadOnly="True"
                         Text="{Binding ActivityLog}"
                         TextWrapping="Wrap"
                         VerticalAlignment="Stretch"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
